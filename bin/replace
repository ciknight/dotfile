#!/usr/bin/env bash

sed
DATE=`date +%s`

until [ $# -eq 0 ]
do
    file=$(echo $1|sed 's/ /?/g')
    if [ -d ~/.trash/$file ] || [ -f ~/.trash/$file ]; then
        mv -v $file ~/.trash/$file.$DATE
    else
        mv -v $file ~/.trash/
    fi
    shift
done
