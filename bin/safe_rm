#!/bin/bash
DATE=`date +%s`

for filename in $@
do
    if [ -d ~/.trash/$filename ] || [ -f ~/.trash/$filename ]; then
        mv $filename ~/.trash/$filename.$DATE
    else
        mv $filename ~/.trash
    fi
done
